import React from "react";
import Layout from "../../components/Layout/NhaTuyenDung/Layout";
import AllowPost from "../../components/AllowPost";

const AllowPostEmployers = () => {
  return (
    <div>
      <Layout>
        Home page của nhà tuyển dụng: Trang này chỉ có chức năng đăng bài. Tuy
        nhiên bạn cũng không thể đăng bài vì đây không phải chức năng tối thiểu.
        Chúng tôi chỉ làm trang mô phỏng. Nếu tài khoản đã được active thì mới
        có thể đăng bài
      </Layout>
    </div>
  );
};

export default AllowPostEmployers;
